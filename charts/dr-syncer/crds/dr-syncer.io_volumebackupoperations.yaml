---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: volumebackupoperations.dr-syncer.io
spec:
  group: dr-syncer.io
  names:
    kind: VolumeBackupOperation
    listKind: VolumeBackupOperationList
    plural: volumebackupoperations
    shortNames:
    - vbo
    singular: volumebackupoperation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Operation type (Backup or Restore)
      jsonPath: .spec.operationType
      name: Type
      type: string
    - description: Source PVC name
      jsonPath: .spec.sourcePVC.name
      name: PVC
      type: string
    - description: Current phase of the operation
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Completion progress percentage
      jsonPath: .status.progressPercentage
      name: Progress
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VolumeBackupOperation tracks an individual backup or restore operation
          for a PVC using the Kopia backup path.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VolumeBackupOperationSpec defines the desired state of VolumeBackupOperation
            properties:
              backupRepositoryRef:
                description: BackupRepositoryRef references the BackupRepository to
                  use
                properties:
                  name:
                    description: Name is the name of the secret
                    type: string
                  namespace:
                    description: Namespace is the namespace of the secret
                    type: string
                required:
                - name
                - namespace
                type: object
              dataAccessStrategy:
                default: Auto
                description: DataAccessStrategy specifies how to access PVC data for
                  backup
                enum:
                - Auto
                - Snapshot
                - Live
                type: string
              destinationPVC:
                description: DestinationPVC references the destination PVC (restore
                  writes to this)
                properties:
                  name:
                    description: Name of the PVC
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the PVC
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
              operationType:
                description: OperationType specifies whether this is a Backup or Restore
                  operation
                enum:
                - Backup
                - Restore
                type: string
              snapshotID:
                description: SnapshotID is the Kopia snapshot ID to restore from (required
                  for Restore operations)
                type: string
              sourcePVC:
                description: SourcePVC references the source PVC (backup reads from
                  this)
                properties:
                  name:
                    description: Name of the PVC
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the PVC
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - backupRepositoryRef
            - operationType
            - sourcePVC
            type: object
          status:
            description: VolumeBackupOperationStatus defines the observed state of
              VolumeBackupOperation
            properties:
              bytesTransferred:
                description: BytesTransferred is the total bytes transferred during
                  the operation
                format: int64
                type: integer
              completionTime:
                description: CompletionTime is when the operation completed (success
                  or failure)
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the operation's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              errorMessage:
                description: ErrorMessage contains error details if the operation
                  failed
                type: string
              kopiaSnapshotID:
                description: KopiaSnapshotID is the Kopia snapshot ID created by a
                  backup operation
                type: string
              message:
                description: Message provides additional status information
                type: string
              phase:
                description: Phase represents the current phase of the operation
                enum:
                - Pending
                - SnapshotCreated
                - DataAccessReady
                - BackupInProgress
                - BackupComplete
                - RestoreInProgress
                - RestoreComplete
                - Failed
                type: string
              progressPercentage:
                description: ProgressPercentage indicates completion progress (0-100)
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              startTime:
                description: StartTime is when the operation started
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
