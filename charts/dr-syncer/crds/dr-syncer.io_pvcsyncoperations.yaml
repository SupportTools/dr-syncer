---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: pvcsyncoperations.dr-syncer.io
spec:
  group: dr-syncer.io
  names:
    kind: PVCSyncOperation
    listKind: PVCSyncOperationList
    plural: pvcsyncoperations
    shortNames:
    - ps
    singular: pvcsyncoperation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.sourceNamespace
      name: Source NS
      type: string
    - jsonPath: .spec.pvcName
      name: PVC
      type: string
    - jsonPath: .spec.destinationNamespace
      name: Dest NS
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.progress
      name: Progress
      type: string
    - jsonPath: .status.duration
      name: Duration
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PVCSyncOperation represents a PVC synchronization operation between source and destination clusters.
          It provides first-class visibility into PVC replication status via kubectl.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PVCSyncOperationSpec defines the specification for a PVC
              sync operation
            properties:
              destinationNamespace:
                description: DestinationNamespace is the target namespace for the
                  sync
                type: string
              destinationPVCName:
                description: DestinationPVCName is the name of the destination PVC
                  (if different from source)
                type: string
              namespaceMappingRef:
                description: NamespaceMappingRef references the parent NamespaceMapping
                  that triggered this sync
                properties:
                  name:
                    description: Name is the name of the NamespaceMapping
                    type: string
                  namespace:
                    description: Namespace is the namespace of the NamespaceMapping
                    type: string
                required:
                - name
                type: object
              pvcName:
                description: PVCName is the name of the PVC being synced
                type: string
              sourceNamespace:
                description: SourceNamespace is the namespace of the source PVC
                type: string
            required:
            - destinationNamespace
            - pvcName
            - sourceNamespace
            type: object
          status:
            description: PVCSyncOperationStatus defines the observed state of a PVC
              sync operation
            properties:
              bytesTransferred:
                description: BytesTransferred is the total bytes transferred
                format: int64
                type: integer
              completionTime:
                description: CompletionTime is when the sync finished
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the PVCSync's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              duration:
                description: Duration is the human-readable duration of the current/last
                  sync (e.g., "5m30s")
                type: string
              error:
                description: Error contains the error message if the sync failed
                type: string
              estimatedRemaining:
                description: EstimatedRemaining is the estimated time remaining (e.g.,
                  "2m15s")
                type: string
              filesTransferred:
                description: FilesTransferred is the total files transferred
                type: integer
              history:
                description: History contains the last few sync attempts (max 5)
                items:
                  description: PVCSyncHistoryEntry records a past sync attempt
                  properties:
                    bytesTransferred:
                      description: BytesTransferred is the total bytes synced
                      format: int64
                      type: integer
                    completionTime:
                      description: CompletionTime is when the sync attempt finished
                      format: date-time
                      type: string
                    duration:
                      description: Duration is the human-readable duration (e.g.,
                        "5m30s")
                      type: string
                    error:
                      description: Error contains any error message for failed syncs
                      type: string
                    filesTransferred:
                      description: FilesTransferred is the total files synced
                      type: integer
                    phase:
                      description: Phase is the final phase of the sync attempt
                      enum:
                      - Pending
                      - Initializing
                      - Syncing
                      - Verifying
                      - Completed
                      - Failed
                      type: string
                    startTime:
                      description: StartTime is when the sync attempt started
                      format: date-time
                      type: string
                  required:
                  - phase
                  - startTime
                  type: object
                type: array
              phase:
                description: Phase is the current phase of the sync operation
                enum:
                - Pending
                - Initializing
                - Syncing
                - Verifying
                - Completed
                - Failed
                type: string
              progress:
                description: Progress is the sync progress as a percentage string
                  (e.g., "75%")
                type: string
              speed:
                description: Speed is the current transfer rate as a human-readable
                  string (e.g., "1.5 MB/s")
                type: string
              startTime:
                description: StartTime is when the current sync started
                format: date-time
                type: string
              totalBytes:
                description: TotalBytes is the expected total bytes (if known)
                format: int64
                type: integer
              totalFiles:
                description: TotalFiles is the expected total files (if known)
                type: integer
              verification:
                description: Verification contains the results of post-sync verification
                properties:
                  checksumMatch:
                    description: ChecksumMatch indicates whether all checksums matched
                    type: boolean
                  error:
                    description: Error contains any verification error message
                    type: string
                  filesTotal:
                    description: FilesTotal is the total number of files checked
                    type: integer
                  filesVerified:
                    description: FilesVerified is the number of files that were verified
                    type: integer
                  mode:
                    description: Mode is the verification mode used (none, sample,
                      full)
                    type: string
                  verifiedAt:
                    description: VerifiedAt is when the verification was completed
                    format: date-time
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
